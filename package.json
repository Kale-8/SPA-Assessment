{
  "name": "kaledmesalopera089",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/js/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:api": "json-server --watch db.json",
    "start": "concurrently \"npm run dev\" \"json-server --watch db.json\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "json-server": "^1.0.0-beta.3",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
